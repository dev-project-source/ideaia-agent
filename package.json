{
  "name": "ideaia-agent",
  "version": "0.1.0",
  "description": "RAG runtime agent for IDEA IA clients — reads KB from Postgres+pgvector, responds via GHL through n8n",
  "private": true,
  "type": "module",
  "scripts": {
    "db:up": "docker compose -f docker/docker-compose.yml up -d",
    "db:down": "docker compose -f docker/docker-compose.yml down",
    "first-activation": "node scripts/first-activation.js",
    "test:conversation": "node scripts/test-conversation.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@upstash/redis": "^1.34.0",
    "dotenv": "^16.4.5",
    "googleapis": "^144.0.0",
    "openai": "^4.67.0",
    "pg": "^8.13.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0"
  }
}
